<Window x:Class="IndexerTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IndexerTest"
        xmlns:wv="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        FlowDirection="RightToLeft"
        FontSize="14"
        Title="pcinfogmach indexer" Height="450" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Margin="5">
            <Grid.Resources>
                <Style TargetType="Button">
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="{Binding BorderBrush, ElementName=SearchBox}"/>
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBox x:Name="SearchBox" 
                     Margin="5" 
                     Padding="3"
                     KeyDown="SearchBox_KeyDown"/>
            <Button x:Name="SearchButton" 
                    Grid.Column="1" 
                    Content="חיפוש" 
                    Click="SearchButton_Click"/>

            <StackPanel Orientation="Horizontal"
                        Grid.Row="1"
                        Grid.ColumnSpan="2">
                <Button x:Name="CreateIndexButton" 
                    Content="צור אינדקס"
                    Click="CreateIndexButton_Click"/>
                <TextBlock Text="ניצול משאבי מחשב:" 
                       VerticalAlignment="Center" Margin="5"/>
                <local:UpDownTextBox x:Name="MemoryUsageBox"
                    Width="40"
                                 Margin="5" Text="10" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                <TextBlock Text="מרחק בין מילים:" 
                            VerticalAlignment="Center" Margin="5"/>
                <local:UpDownTextBox x:Name="AdjacencySettingsBox"
                    Width="40"
                    Margin="5" Text="2" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
                <!--<Button x:Name="DebugButton" 
                    Grid.Column="3" 
                    Content="Debug"
                    Click="DebugButton_Click"/>-->
            </StackPanel>
        </Grid>

        <Separator Grid.Row="1"/>
        
        <wv:WebView2 x:Name="WebView" 
                     Grid.Row="2"/>
    </Grid>
</Window>
